
                  
// headers.append('Authorization', 'Basic ' + base64.encode(username + ":" + password));
const constant = {
   //databaseUrl: 'https://guarded-anchorage-82080.herokuapp.com'
  databaseUrl: 'http://localhost:8080'
}
export default constant;
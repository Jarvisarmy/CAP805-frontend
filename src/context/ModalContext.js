import {createContext} from 'react';


//create the context
const ModalContext = createContext();


//when we export the context , it means we can use the context whereever we want to use it.
export default ModalContext;